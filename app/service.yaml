# apiVersion: v1
# kind: Service
# metadata:
#  name: wil-playground
# spec:
#  ports:
#  - port: 80
#    name: http
#    protocol: TCP
#    targetPort: 8080
#    nodePort: 8888
#  selector:
#    app: wil-playground
#  type: NodePort

apiVersion: v1
kind: Service
metadata:
  labels:
    app: wil-playground
  name: wil-playground-service
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: wil-playground
  ports:
  - name: http
    port: 8888
    protocol: TCP
    targetPort: 8888

